# network-ping-monitor
Python network monitoring script for ping and DNS resolution
# Network Ping Monitor

Python script for:
- Reading hosts from Excel
- DNS resolution
- Ping monitoring
- IP validation

## Features
- Resolve hostname to IP
- Ping hosts
- Detect invalid IPs

## Requirements
pip install -r requirements.txt
